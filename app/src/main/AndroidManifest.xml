<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.longnh.fooddelivery">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodDelivery"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity
            android:name=".ui.splashPage.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.FoodDelivery.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.mainPage.HomeActivity"
            android:exported="false">

        </activity>
        <activity android:name=".ui.cartPage.CartActivity" />
        <activity android:name=".ui.popularFoodPage.PopularFoodDetailActivity" />
        <activity android:name=".ui.recommendedFoodPage.RecommendedFoodDetailActivity" />
    </application>

</manifest>